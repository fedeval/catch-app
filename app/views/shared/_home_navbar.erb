<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" style="justify-content: flex-end; background: none; padding: 23px 16px;">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <% if current_user.photo.attached? %>      
            <%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown talkbubble", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %></p>
          <% else %>
            <%= image_tag "hektor.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown talkbubble", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <% end %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Dashboard", dashboard_index_path(view: "contacts"), class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
